apiVersion: 1
datasources:
- name: PostgreSQL-postgres
  type: postgres
  access: proxy
  url: postgres:5432
  database: postgres
  user: postgres
  secureJsonData:
    password: postgres
  jsonData:
    sslmode: disable
    postgresVersion: 1400
  editable: true
