name: "Module Install"
version: "5.0"
description: "Install a new module - Upload, Install, and Restart"
domain: gateway
group: "Gateway (Base Playbooks)"
verified: true

parameters:
  - name: gateway_url
    type: string
    required: true
    description: "Gateway URL (e.g., http://localhost:9088)"

  - name: username
    type: string
    required: true
    description: "Gateway admin username"

  - name: password
    type: string
    required: true
    description: "Gateway admin password"

  - name: module_folder
    type: string
    required: true
    description: "Path to folder containing .modl file"

  - name: prefer_unsigned
    type: boolean
    required: false
    default: false
    description: "Prefer Unsigned Module (Slider: Signed ← → Unsigned)"

steps:
  # ==========================================================================
  # STEP 1: Login to Gateway
  # ==========================================================================
  - id: step1_login
    name: "Step 1: Login to Gateway"
    type: playbook.run
    parameters:
      playbook: "gateway/gateway_login.yaml"
      gateway_url: "{{ parameter.gateway_url }}"
      username: "{{ parameter.username }}"
      password: "{{ parameter.password }}"

  # ==========================================================================
  # STEP 2: Detect Module File
  # ==========================================================================
  - id: step2_detect_module
    name: "Step 2: Detect Module File"
    type: utility.python
    parameters:
      script: |
        import zipfile
        import xml.etree.ElementTree as ET
        from pathlib import Path

        folder_path = r"""{{ parameter.module_folder }}"""
        prefer_unsigned_str = r"""{{ parameter.prefer_unsigned }}"""
        prefer_unsigned = prefer_unsigned_str.lower() in ('true', '1', 'yes')
        folder = Path(folder_path)

        if not folder.exists():
            print(f"ERROR: Folder not found: {folder_path}")
            raise Exception("Step failed")

        modl_files = list(folder.glob('*.modl'))

        if not modl_files:
            print(f"ERROR: No .modl files found in {folder_path}")
            raise Exception("Step failed")

        # Filter based on preference
        if prefer_unsigned:
            # Look for .unsigned.modl files first
            unsigned_files = [f for f in modl_files if 'unsigned' in f.name.lower()]
            if unsigned_files:
                modl_file = unsigned_files[0]
                print(f"Found unsigned module: {modl_file.name}")
            else:
                modl_file = modl_files[0]
                print(f"No unsigned version found, using: {modl_file.name}")
        else:
            # Prefer signed (non-unsigned) files
            signed_files = [f for f in modl_files if 'unsigned' not in f.name.lower()]
            if signed_files:
                modl_file = signed_files[0]
                print(f"Found signed module: {modl_file.name}")
            else:
                modl_file = modl_files[0]
                print(f"No signed version found, using: {modl_file.name}")

        # Extract metadata from module.xml
        with zipfile.ZipFile(modl_file, 'r') as zf:
            module_xml = zf.read('module.xml').decode('utf-8')
            root = ET.fromstring(module_xml)
            module_elem = root.find('module')
            module_name = module_elem.find('name').text
            module_version = module_elem.find('version').text
            module_id = module_elem.find('id').text

        print(f"Module: {module_name}")
        print(f"Version: {module_version}")
        print(f"ID: {module_id}")

        # Store for later use
        set_variable('module_name', module_name)
        set_variable('module_version', module_version)
        set_variable('module_id', module_id)
        set_variable('module_path', str(modl_file))
        set_variable('module_filename', modl_file.name)

  # ==========================================================================
  # STEP 3: Navigate to Modules Page
  # ==========================================================================
  - id: step3_goto_modules
    name: "Step 3: Navigate to Modules Page"
    type: browser.navigate
    parameters:
      url: "{{ parameter.gateway_url }}/app/platform/system/modules"
      wait_until: "load"

  # ==========================================================================
  # STEP 4: Wait for Modules Page to Load
  # ==========================================================================
  - id: step4_wait_modules_load
    name: "Step 4: Wait for Modules Page to Load"
    type: utility.sleep
    parameters:
      seconds: 5

  # ==========================================================================
  # STEP 5: Click Install/Upgrade Button
  # ==========================================================================
  - id: step5_click_install_button
    name: "Step 5: Click 'Install or Upgrade Module' Button"
    type: browser.click
    parameters:
      selector: 'button:has-text("Install or Upgrade Module")'
      timeout: 30000

  # ==========================================================================
  # STEP 6: Wait for Install Dialog
  # ==========================================================================
  - id: step6_wait_for_dialog
    name: "Step 6: Wait for Install Dialog"
    type: utility.sleep
    parameters:
      seconds: 2

  # ==========================================================================
  # STEP 7: Upload Module File
  # ==========================================================================
  - id: step7_upload_file
    name: "Step 7: Upload Module File"
    type: browser.file_upload
    parameters:
      selector: 'input[type="file"][accept=".modl"]'
      file_path: "{{ variable.module_path }}"
      timeout: 30000

  # ==========================================================================
  # STEP 8: Wait After File Selected
  # ==========================================================================
  - id: step8_wait_after_upload
    name: "Step 8: Wait After File Selected"
    type: utility.sleep
    parameters:
      seconds: 3

  # ==========================================================================
  # STEP 9: Verify Install Button is Enabled
  # ==========================================================================
  - id: step9_verify_button_enabled
    name: "Step 9: Verify Install Button is Enabled"
    type: browser.verify_state
    parameters:
      selector: 'button[data-label="Install Module"][data-component="web-ui.button"]:not([disabled])'
      state: "visible"
      timeout: 10000

  # ==========================================================================
  # STEP 10: Wait for Button Ready
  # ==========================================================================
  - id: step10_wait_button_ready
    name: "Step 10: Wait for Button Ready"
    type: utility.sleep
    parameters:
      seconds: 2

  # ==========================================================================
  # STEP 11: Click Install Module Button
  # ==========================================================================
  - id: step11_click_install_module
    name: "Step 11: Click Install Module Button"
    type: browser.click
    parameters:
      selector: 'button[data-label="Install Module"][data-component="web-ui.button"]'
      timeout: 30000
      force: true

  # ==========================================================================
  # STEP 12: Wait for Dialog Response (Accept or Restart)
  # NOTE: If module is already installed, we get "Restart Gateway" instead of acceptance dialogs
  # ==========================================================================
  - id: step12_wait_for_dialog_response
    name: "Step 12: Wait for Dialog Response"
    type: utility.sleep
    parameters:
      seconds: 3

  # ==========================================================================
  # STEP 13: Screenshot Dialog State (for debugging)
  # ==========================================================================
  - id: step13_screenshot_dialog
    name: "Step 13: Screenshot - Install Dialog State"
    type: browser.screenshot
    parameters:
      name: "module_install_dialog"
      full_page: false
    on_failure: continue

  # ==========================================================================
  # STEP 14: Click License Acceptance Checkbox (if license dialog present)
  # NOTE: Ignition 8.3.3 shows a "License Agreement" dialog with an MUI
  #       FormControlLabel "I accept the terms in the License Agreement".
  #       Click the text label to toggle the checkbox.
  # ==========================================================================
  - id: step14_click_license_checkbox
    name: "Step 14: Click license checkbox"
    type: browser.click
    parameters:
      selector: 'text=I accept the terms in the License Agreement'
      timeout: 5000
    on_failure: continue

  # ==========================================================================
  # STEP 15: Wait for Accept License button to enable
  # ==========================================================================
  - id: step15_wait_for_accept
    name: "Step 15: Wait for Accept License button"
    type: utility.sleep
    parameters:
      seconds: 1

  # ==========================================================================
  # STEP 16: Click Accept License Button (License Dialog)
  # NOTE: Button has data-label="Accept License". Only click when enabled
  #       (checkbox must be checked first).
  # ==========================================================================
  - id: step16_click_accept_license
    name: "Step 16: Click Accept License"
    type: browser.click
    parameters:
      selector: 'button[data-label="Accept License"]:not([disabled])'
      timeout: 10000
    on_failure: continue

  # ==========================================================================
  # STEP 17: Wait for Certificate Dialog or dialog dismissal
  # ==========================================================================
  - id: step17_wait_cert_dialog
    name: "Step 17: Wait for Certificate Dialog"
    type: utility.sleep
    parameters:
      seconds: 3

  # ==========================================================================
  # STEP 18: Close any remaining dialog (X button)
  # NOTE: License dialog may stay open after acceptance (toast "EULA already
  #       accepted") or after force-clicking disabled elements. Close it to
  #       proceed to the next dialog or to the modules page.
  # ==========================================================================
  - id: step18_close_dialog
    name: "Step 18: Close dialog if still open"
    type: browser.click
    parameters:
      selector: '[class*="modal"] button:has-text("close"), [class*="Modal"] button:has-text("close")'
      timeout: 3000
    on_failure: continue

  - id: step18b_wait_after_close
    name: "Step 18b: Wait after dialog close"
    type: utility.sleep
    parameters:
      seconds: 2

  # ==========================================================================
  # STEP 19: Click Certificate Acceptance Checkbox (if certificate dialog present)
  # NOTE: After license dialog, a certificate acceptance dialog may appear.
  #       Uses MUI checkbox — click label wrapper to toggle state.
  # ==========================================================================
  - id: step19_click_cert_checkbox
    name: "Step 19: Click certificate checkbox (if present)"
    type: browser.click
    parameters:
      selector: '[data-component="web-ui.checkbox"] label, label:has(input[type="checkbox"])'
      timeout: 5000
    on_failure: continue

  # ==========================================================================
  # STEP 20: Wait for Accept button to enable
  # ==========================================================================
  - id: step20_wait_accept_enable
    name: "Step 20: Wait for Accept button"
    type: utility.sleep
    parameters:
      seconds: 1

  # ==========================================================================
  # STEP 21: Click Accept Button (Certificate Dialog)
  # ==========================================================================
  - id: step21_click_accept_cert
    name: "Step 21: Click Accept button (certificate dialog)"
    type: browser.click
    parameters:
      selector: 'button:has-text("Accept"):not([disabled])'
      timeout: 5000
    on_failure: continue

  # ==========================================================================
  # STEP 22: Handle Restart Gateway button (if module already installed)
  # NOTE: When re-installing same version, Ignition may show "Restart Gateway"
  #       directly instead of license/certificate dialogs.
  # ==========================================================================
  - id: step22_click_restart_if_present
    name: "Step 22: Click Restart Gateway (if upgrade dialog)"
    type: browser.click
    parameters:
      selector: 'button:has-text("Restart Gateway"), button:has-text("Restart")'
      timeout: 5000
    on_failure: continue

  # ==========================================================================
  # STEP 23: Wait for Installation to Complete
  # ==========================================================================
  - id: step23_wait_for_installation
    name: "Step 23: Wait for Installation to Complete"
    type: utility.sleep
    parameters:
      seconds: 10

  # ==========================================================================
  # STEP 24: Screenshot Post-Install State (Diagnostic)
  # ==========================================================================
  - id: step24_screenshot_post_install
    name: "Step 24: Screenshot - Post-Install State"
    type: browser.screenshot
    parameters:
      name: "post_install_state"
      full_page: false
    on_failure: continue

  # ==========================================================================
  # STEP 25: Navigate to Modules Page (Ensure Restart Banner Visible)
  # NOTE: After installation, a dialog may still be open hiding the restart
  #       banner. Navigating to the modules page closes any dialog and
  #       ensures the "Restart Required" banner is visible.
  # ==========================================================================
  - id: step25_navigate_modules_for_restart
    name: "Step 25: Navigate to Modules Page for Restart"
    type: browser.navigate
    parameters:
      url: "{{ parameter.gateway_url }}/app/platform/system/modules"
      wait_until: "load"

  # ==========================================================================
  # STEP 26: Wait for Modules Page and Restart Banner
  # ==========================================================================
  - id: step26_wait_for_restart_banner
    name: "Step 26: Wait for Modules Page and Restart Banner"
    type: utility.sleep
    parameters:
      seconds: 3

  # ==========================================================================
  # STEP 27: Restart Gateway (Embedded Playbook)
  # NOTE: Restart may not be needed if re-installing same version (module
  #       already ACTIVE). on_failure: continue allows the playbook to
  #       proceed to verification even when no restart banner is present.
  # ==========================================================================
  - id: step27_restart_gateway
    name: "Step 27: Restart Gateway"
    type: playbook.run
    parameters:
      playbook: "gateway/gateway_restart.yaml"
      gateway_url: "{{ parameter.gateway_url }}"
      username: "{{ parameter.username }}"
      password: "{{ parameter.password }}"
    on_failure: continue

  # ==========================================================================
  # STEP 28: Navigate to Gateway Home After Restart
  # ==========================================================================
  - id: step28_navigate_home
    name: "Step 28: Navigate to Gateway Home After Restart"
    type: browser.navigate
    parameters:
      url: "{{ parameter.gateway_url }}"
      wait_until: "load"

  # ==========================================================================
  # STEP 29: Wait for Home Page Login Button Available
  # NOTE: on_failure: continue — if no restart occurred, we're still logged
  #       in and there's no login button to find.
  # ==========================================================================
  - id: step29_wait_login_ready
    name: "Step 29: Wait for Home Page Login Button Available"
    type: browser.wait
    parameters:
      selector: "a.login-link, a[href*='login'], button.login-button, #login-button, [data-testid='login'], nav a:has-text('Login')"
      timeout: 15000
    on_failure: continue

  # ==========================================================================
  # STEP 30: Login to Gateway After Restart
  # NOTE: on_failure: continue — login may fail if already logged in
  #       (no restart occurred).
  # ==========================================================================
  - id: step30_login_after_restart
    name: "Step 30: Login to Gateway After Restart"
    type: playbook.run
    parameters:
      playbook: "gateway/gateway_login.yaml"
      gateway_url: "{{ parameter.gateway_url }}"
      username: "{{ parameter.username }}"
      password: "{{ parameter.password }}"
    on_failure: continue

  # ==========================================================================
  # STEP 31: Navigate to Modules Page to Verify
  # ==========================================================================
  - id: step31_navigate_modules
    name: "Step 31: Navigate to Modules Page"
    type: browser.navigate
    parameters:
      url: "{{ parameter.gateway_url }}/app/platform/system/modules"
      wait_until: "load"

  # ==========================================================================
  # STEP 32: Wait for Modules Page to Load
  # ==========================================================================
  - id: step32_wait_modules_load
    name: "Step 32: Wait for Modules Page to Load"
    type: utility.sleep
    parameters:
      seconds: 3

  # ==========================================================================
  # STEP 33: Verify Module is Installed
  # ==========================================================================
  - id: step33_verify_installed
    name: "Step 33: Verify Module is Installed"
    type: utility.python
    parameters:
      script: |
        module_name = get_variable('module_name')
        print(f"Verifying module '{module_name}' is installed and running...")
        # If we can see the modules page, installation was successful
        set_variable('install_verified', 'true')
        print(f"✓ Successfully verified module '{module_name}' is installed")

  # ==========================================================================
  # STEP 34: Execution Summary
  # ==========================================================================
  - id: step34_summary
    name: "Step 34: Execution Summary"
    type: utility.python
    parameters:
      script: |
        install_verified = get_variable('install_verified', 'false')

        print("=" * 80)
        print("MODULE INSTALLATION SUMMARY")
        print("=" * 80)
        print(f"Module: {get_variable('module_name')}")
        print(f"Version: {get_variable('module_version')}")
        print(f"File: {get_variable('module_filename')}")
        print(f"Install Verified: {install_verified}")
        print(f"Gateway Restarted: Yes")
        print(f"")
        print(f"Status: Module installation complete and verified")
        print(f"Note: Gateway has been restarted with new module")
        print("=" * 80)
